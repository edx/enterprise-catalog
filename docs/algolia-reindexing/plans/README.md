## Implementation Plan Records

The plans in this directory are implementation plans developed (usually) by AI coding assistants
and are intended for consumption by AI coding agents.

We persist them during build for portability.

They are persisted after build is complete mostly as historical artifacts, not sources of truth. It's
possible that they can help shed light on motivation.
